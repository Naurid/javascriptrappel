const pi = 3.14;

let user = {
    name: 'Vladimir',
    hobby:'throwing people out of windows',
    favoriteFood : 'poutine'
};

console.log(pi);
console.log(`${user.name} likes ${user.hobby} and ${user.favoriteFood}`);

console.log(user);